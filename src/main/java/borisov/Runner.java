package borisov;

import borisov.api.Init;

import java.util.Arrays;

public class Runner {
    public static void main(String[] args) {
        Init initializer = new Init();
        initializer.initialize();
       // System.out.println(initializer.map.toString());
    }
}
